name: Tests

on:
  push:
  
jobs:
  basic-job:
    name: run tests
    runs-on: ubuntu-latest  
    
    steps:
        
      - name: Run a simple command
        run: echo "pretend tests are running lol"
